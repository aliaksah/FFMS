% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{fms.parallel}
\alias{fms.parallel}
\title{Run Multiple FMS Runs in Parallel, Merging the Results Before Returning.}
\usage{
fms.parallel(runs = 2, cores = getOption("mc.cores", 2L), ...)
}
\arguments{
\item{runs}{The number of runs to run}

\item{cores}{The number of cores to run on}

\item{...}{Further parameters passed to fms_base.}
}
\value{
Merged results from multiple fms_base runs
}
\description{
Run Multiple FMS Runs in Parallel, Merging the Results Before Returning.
}
\examples{
result <- fms.parallel(runs = 1, 
cores = 1, 
loglik.pi = FFMS::gaussian.loglik,
y = matrix(rnorm(100), 100),
x = matrix(rnorm(600), 100))
summary(result)
plot(result)

}
